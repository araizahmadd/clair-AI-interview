# Interview agent stack (Gemini resume + Tavily + LangGraph + optional Cartesia voice + LangSmith)
langgraph>=0.2.55
google-genai>=1.38.0
tavily-python>=0.5.0
pydantic>=2.0
python-dotenv>=1.0
langsmith>=0.4.31
websockets>=12.0
streamlit>=1.38.0

# Voice (optional): Cartesia microphone/speaker session
pyaudio>=0.2.14

# Emotion monitor (optional but enabled by default with --voice unless --no-emotion)
# If these fail on your Python version, use the dedicated setup in requirements-emotion.txt.
opencv-python>=4.8
torch
timm>=0.9,<0.10
hsemotion>=0.2.0
